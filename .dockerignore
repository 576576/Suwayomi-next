# 构建上下文只留「编 server + 打 webui/jre」需要的东西。
#
# 扩展沙盒 jar 不再是本地 gradle 构建的 —— 改为从 Suwayomi-ext-runtime 的
# Release 资产下载（见 Dockerfile 的 sandbox stage），所以上下文里不再需要任何
# Gradle 工程或共享源码目录。

# 本地产物与缓存。必须写 `**/`：裸 `target/` 只匹配上下文根下的那一层，
# 子目录里的构建产物否则会被整个传给 docker daemon。
**/target/
**/build/
**/.gradle/
bin/

# 不参与镜像构建的东西
.git
.github
.idea
.workbuddy
docs/
*.md

# 运行期数据（安装的扩展、本地图源），不该进镜像
extensions/
data/
