# AndroidCompat 的 `ConfigManager` 构造时无条件读 `server.debugLogsEnabled`
# （`Logging.kt::debugLogsEnabled` —— 默认值是**先求值**的实参，`System.getProperty`
# 里的 `-D` 覆盖绕不过去），而真正的 `server-reference.conf` 属 server 侧，
# 不在扩展沙盒的 classpath 上。这里只补这一个键，不是服务器默认配置。
server.debugLogsEnabled = false
